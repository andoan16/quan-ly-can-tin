{
  "providers": ["node"],
  "build": {
    "rootDirectory": "apps/backend",
    "commands": {
      "build": "npm run build",
      "install": "npm install"
    }
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}